import { StyleSheet} from 'react-native';

const linkBarStyles = StyleSheet.create({
  container: {
    flex: 1,
    flexDirection: 'row',
    justifyContent: 'space-around',
  },
});

export { linkBarStyles };