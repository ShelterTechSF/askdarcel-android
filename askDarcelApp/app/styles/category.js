import { StyleSheet } from 'react-native';

const categoryStyles = StyleSheet.create({
  name: {
    fontSize: 20,
    textAlign: 'center',
    margin: 10,
  },
  image: {
    height: 100,
    width: 100,
  }
});

export { categoryStyles };
